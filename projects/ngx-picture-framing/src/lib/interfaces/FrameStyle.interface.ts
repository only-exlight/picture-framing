export interface FrameStyle {
  background: 'dotted' | 'dashed' | 'solid' | 'double' | 'groove' | 'ridge' | 'inset';
  borderColor: string;
  borderActive: string;
  borderStyle: string;
  borderSize: number;
}
