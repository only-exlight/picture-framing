export interface FrameStyle {
  background: string;
  borderColor: string;
  borderActive: string;
  borderStyle: 'dotted' | 'dashed' | 'solid' | 'double' | 'groove' | 'ridge' | 'inset';
  borderSize: number;
}
